# Server-side only. Copy to .env for local use; .env is ignored by Git.
# Leave the key empty to use the deterministic fixture workflow without a live model.
HERLENS_API_KEY=
HERLENS_BASE_URL=
HERLENS_MODEL=

# Never rename a secret to VITE_*: Vite embeds those variables in browser code.
